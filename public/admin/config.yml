# backend:
#   name: github
#   repo: "your-username/your-repo"
#   branch: main
#   # base_url and auth_endpoint will be set after you configure your OAuth proxy
# site_url: "https://your-site.com" # the URL of your site

# media_folder: "public/images/uploads"
# public_folder: "/images/uploads"

# collections:
#   - name: "pages"
#     label: "Pages"
#     folder: "src/pages" # the folder where your CMS managed Astro pages are located
#     create: true
#     slug: "{{slug}}"
#     fields:
#       - { label: "Title", name: "title", widget: "string" }
#       - { label: "Body", name: "body", widget: "markdown" }

backend:
  name: github
  repo: "kamikazeUKYO/astro-decap-cloudflare-starter"   # ← あなたのリポジトリ
  branch: main
  base_url: "https://decap-oauth-ukyo.snakesnake1981.workers.dev/"  # ← 作成後に実URLを入れる
  auth_endpoint: "/auth"

site_url: "https://astro-decap-cloudflare-starter.pages.dev/"

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "pages"
    label: "固定ページ"
    folder: "src/pages/pages"
    create: true
    slug: "{{slug}}"
    fields:
      - { label: "タイトル", name: "title", widget: "string" }
      - { label: "本文", name: "body", widget: "markdown" }

  - name: "posts"
    label: "ブログ"
    folder: "src/pages/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "タイトル", name: "title", widget: "string" }
      - { label: "公開日", name: "pubDate", widget: "datetime" }
      - { label: "本文", name: "body", widget: "markdown" }
